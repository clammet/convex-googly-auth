# Changesets

Run `npm run changeset` for every user-visible change. Select the semantic
version bump and describe the change in consumer-facing language. Commit the
generated Markdown file with the change.

Changes that only affect tests, documentation, or repository tooling do not need
a changeset unless they alter the published package or its usage.
